# Build output
target/
dependency-reduced-pom.xml

# Keep the release JAR in the repo root
!sip-loadtester.jar

# IDE files
.classpath
.project
.settings/
.idea/
*.iml

# Environment / secrets
.env
.env.*

# SIP log files (may contain auth traces)
*.log
